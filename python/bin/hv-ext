#!/usr/bin/env python3
"""
hv-extra: Extended utilities for hotvect ML operations

This tool provides additional command-line utilities that support
hotvect workflows but are not part of the core functionality.
"""

import os
import sys

# Add the parent directory to the Python path so we can import hotvect modules
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from hotvect.extra.cli import main  # noqa: E402

if __name__ == "__main__":
    main()
